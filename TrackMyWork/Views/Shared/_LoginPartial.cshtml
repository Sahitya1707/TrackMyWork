@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
        <li class="border-b-2 durtion-50 my-[20px] w-[100%] border-solid border-Primary p-2 uppercase ease-in hover:bg-Secondary hover:bg-opacity-30">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button type="submit" class="nav-link btn btn-link text-dark uppercase">Logout</button>
            </form>
        </li>
        <li class="durtion-50 my-[20px] flex w-[100%] items-center gap-x-2 overflow-hidden text-wrap p-2 normal-case ease-in">
            <!--
            <a  class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">@User.Identity?.Name!</a>


            -->
            <p class="border-2 flex items-center justify-center rounded-full border-solid border-ColorWhite p-[2px] text-lg">
                <span class="material-symbols-outlined"
                style="
font-size:inherit;
"
                >
                    person
                </span>
            </p>
            <span>
                @User.Identity?.Name
            </span>
          
    </li>

}
else
    {
        <li class="border-b-2 durtion-50 my-[20px] w-[100%] border-solid border-Primary p-2 ease-in hover:bg-Secondary hover:bg-opacity-30">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
        </li>
        <li class="border-b-2 durtion-50 my-[20px] w-[100%] border-solid border-Primary p-2 ease-in hover:bg-Secondary hover:bg-opacity-30">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
      
}
</ul>
